version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - TODOS_FILE=/app/data/todos.json
    volumes:
      - ./data:/app/data
